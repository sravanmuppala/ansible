- name: variables
  hosts: localhost
  vars_files:
  - vars.yaml
  tasks:
  - name: Print Information
    ansible.builtin.debug:
      msg: " Hi, I am learning {{ Course }} from trainer {{ Trainer }} with duration of {{ Duration }} "